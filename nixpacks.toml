[phases.setup]
nixPkgs = ["poppler_utils", "gcc", "zlib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --worker-tmp-dir /dev/shm app:app"
